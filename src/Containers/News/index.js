import React from 'react';
import { Text, View } from 'react-native';

const NewScreen = () => (
    <View style={{ flex: 1 }}>
        <Text>NewScreen</Text>
    </View>
);

export default NewScreen;
